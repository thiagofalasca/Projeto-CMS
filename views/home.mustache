<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Metadados -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Estilos CSS e Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/default.css"> <!-- Estilo padrão -->
    <link rel="stylesheet" href="styles/home.css"> <!-- Estilo específico -->
    <!-- Título da página -->
    <title>Home</title>
</head>

<body class="bg-pastel">
    <!-- Cabeçalho com botão de login -->
    <header class="p-3 d-flex justify-content-end">
        <a href="/login" class="btn bg-orange color-white">Login</a>
    </header>
    <!-- Texto de boas vindas -->
    <div class="text-center">
        <!-- Logo e título -->
        <img class="img-fluid mx-auto d-block" src="images/logo.png" alt="Logo" width="250px">
        <h1 class="fw-bold display-5">Bem vindo ao Prato Feito</h1>
        <p>Seu site preferido para receitas!</p>
    </div>
    <!-- Seção de receitas -->
    <section class="mt-5 p-4 bg-orange d-flex justify-content-around">
        <!-- Card de receitas -->
        {{#pages}}
        <div class="card bg-pastel" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">{{title}}</h5>
                <p class="card-text">{{content}}</p>
                <a href="pages/{{URL}}" class="card-link">link da receita</a>
            </div>
        </div>
        {{/pages}}
    </section>
</body>

</html>