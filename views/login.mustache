<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Metadados -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Estilos CSS e Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/default.css"> <!-- Estilo padrão -->
    <link rel="stylesheet" href="styles/login.css"> <!-- Estilo específico -->
    <!-- Título da página -->
    <title>Login</title>
</head>

<body class="bg-pastel">
    <!-- Logo -->
    <img class="m-auto mb-5 d-block" src="images/logo.png" alt="Logo" width="400px">
    <!-- Formulário de login -->
    <form class="m-auto shadow rounded p-5 bg-white" action="/login" method="post">
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
        <!-- Título do formulário -->
        <h1 class="text-center fw-bold mb-4 color-orange">Login</h1>
        <!-- Campo usuário -->
        <div class="mb-3">
            <label for="user">Usuário</label>
            <input id="user" name="user" type="text" class="form-control" />
        </div>
        <!-- Campo senha -->
        <div class="mb-3">
            <label for="password">Senha</label>
            <input id="password" name="password" type="password" class="form-control" />
        </div>
        <!-- Exibe mensagem de erro, se houver -->
        {{#hasError}}
        <div class="alert alert-danger" role="alert">
            {{#error}}
            <div>{{.}}</div>
            {{/error}}
        </div>
        {{/hasError}}
        <!-- Botão de login e home -->
        <button type="submit" class="btn btn-lg w-100 bg-orange color-white">Entrar</button>
        <a href="/" class="d-block text-center mt-4 text-decoration-none color-orange">Voltar para a tela inicial</a>
    </form>
</body>

</html>